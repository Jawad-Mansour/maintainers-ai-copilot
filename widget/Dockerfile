# Phase 1 stub: serves a static placeholder via nginx.
# Phase 5 replaces this with a multi-stage React + Vite + Tailwind build.
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 80
